# For imgui
apt install -y libxinerama-dev libxcursor-dev xorg-dev libglu1-mesa-dev pkg-config

Build targets:
- `type_erasure`: terminal version
- `map_builder`: map editor GUI
- `game_gui`: playable GUI
